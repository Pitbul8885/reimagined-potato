name: Jekyll site CI

on:
  push:
    branches: [ "codespace-reimagined-potato-5gvj9xq59x9j2vrv" ]
  pull_request:
    branches: [ "codespace-reimagined-potato-5gvj9xq59x9j2vrv" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the site in the jekyll/builder container
      run: |
        docker run \
        -v г${{ github.workspace }}:/srv/jekyll -v ${{ github.workspace }}/_site:/srv/jekyll/_site \
        jekyll/builder:latest /bin/bash -c "chmod -R 777 /srv/jekyll && jekyll build --future"
